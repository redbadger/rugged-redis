source 'https://rubygems.org'

# Specify your gem's dependencies in rugged-redis.gemspec
gemspec

# TODO
# when backends work is done change to 'backends' branch
# when rugged accepts the PR, switch to the mainline development
# when they do a release, move this to gem dependencies and release a version of our own.
gem "rugged", git: 'https://github.com/redbadger/rugged.git', branch: 'backends-wip', submodules: true
